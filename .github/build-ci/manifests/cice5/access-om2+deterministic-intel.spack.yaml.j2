spack:
  specs:
    - 'access-om2@=stable +deterministic %{{ intel_compiler }}'
  packages:
    - '{{ package }}'
    all:
      require:
      - '%{{ intel_compiler }}'
  concretizer:
    unify: false
  view: false
